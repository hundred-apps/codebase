{
    "private": true,

    "scripts": {

        "watch": "mix watch --hot",

        "development": "mix",
        "production": "mix --production"
    },

    "dependencies": {},
    "devDependencies": { "laravel-mix": "^6.0" }
}
